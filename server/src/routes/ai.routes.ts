import { Router } from 'express'
import { notImplemented } from './notImplemented.js'

/**
 * AI routes (Task 2): POST /api/ai/chat, GET /api/ai/models.
 * Placeholder for now.
 */
const router = Router()

router.all('*', notImplemented)

export default router
