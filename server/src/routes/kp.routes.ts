import { Router } from 'express'
import { notImplemented } from './notImplemented.js'

/**
 * KP Agent routes (Task 3): POST /api/kp/invoke.
 * Placeholder for now.
 */
const router = Router()

router.all('*', notImplemented)

export default router
