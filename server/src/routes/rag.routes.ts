import { Router } from 'express'
import { notImplemented } from './notImplemented.js'

/**
 * RAG routes (Task 3): /api/rag* (health/index/query/context/usergraph...).
 * Placeholder for now.
 */
const router = Router()

router.all('*', notImplemented)

export default router
