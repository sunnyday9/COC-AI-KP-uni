import { Router } from 'express'
import { notImplemented } from './notImplemented.js'

/**
 * Save routes (Task 5): GET/PUT/DELETE /api/saves*.
 * Placeholder for now.
 */
const router = Router()

router.all('*', notImplemented)

export default router
