import { Router } from 'express'
import { notImplemented } from './notImplemented.js'

/**
 * Script library routes (Task 4): GET/PUT/DELETE /api/scripts*, POST upload.
 * Placeholder for now.
 */
const router = Router()

router.all('*', notImplemented)

export default router
