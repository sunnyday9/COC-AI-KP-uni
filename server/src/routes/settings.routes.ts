import { Router } from 'express'
import { notImplemented } from './notImplemented.js'

/**
 * Settings routes (Task 2): GET/PUT /api/settings (替代 electron-store).
 * Placeholder for now.
 */
const router = Router()

router.all('*', notImplemented)

export default router
