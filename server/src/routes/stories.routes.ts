import { Router } from 'express'
import { notImplemented } from './notImplemented.js'

/**
 * Story/file routes (Task 4): GET /api/stories*, POST /api/stories/upload, DELETE.
 * Placeholder for now.
 */
const router = Router()

router.all('*', notImplemented)

export default router
